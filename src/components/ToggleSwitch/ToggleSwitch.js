import styled from "styled-components";

const ToggleSwitch = () => {
  const switchTheme = (event) => {
    event.PreventDefault();
  };

  return <Wrapper>React toogle</Wrapper>;
};
const Wrapper = styled.div`
  display: flex;
`;

export default ToggleSwitch;
